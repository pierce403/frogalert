[package]
name = "frogalert-ffi"
version = "0.3.0-beta.1"
edition = "2021"
license = "Apache-2.0"
description = "Narrow C ABI for the Rust FrogAlert application"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
frogalert-core = { path = "../frogalert-core" }
