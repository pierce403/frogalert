{
  "name": "frogalert-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "./scripts/verify",
    "serve": "./scripts/serve-site"
  }
}
